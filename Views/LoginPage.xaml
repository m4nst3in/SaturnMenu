<Page x:Class="SaturnLoader.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="560" d:DesignWidth="800"
      Background="{StaticResource PrimaryDarkBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Login Panel -->
        <Border Grid.Column="1" 
                Background="{StaticResource SecondaryDarkBrush}" 
                CornerRadius="15"
                Padding="40,50"
                Effect="{StaticResource CardStyle.Effect}">
            <StackPanel>
                <!-- Logo -->
                <TextBlock Text="SATURN" 
                          FontSize="32" 
                          FontWeight="Bold" 
                          Foreground="{StaticResource TextPrimaryBrush}"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>
                
                <TextBlock Text="LOADER" 
                          FontSize="16" 
                          Foreground="{StaticResource TextSecondaryBrush}"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,40"/>
                
                <!-- Username Field -->
                <TextBlock Text="Username" 
                          FontSize="14" 
                          Foreground="{StaticResource TextSecondaryBrush}"
                          Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameTextBox" 
                        Style="{StaticResource ModernTextBoxStyle}"
                        Text="admin"
                        Padding="15,12"
                        Margin="0,0,0,20"/>
                
                <!-- Password Field -->
                <TextBlock Text="Password" 
                          FontSize="14" 
                          Foreground="{StaticResource TextSecondaryBrush}"
                          Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" 
                           Style="{StaticResource ModernTextBoxStyle}"
                           Password="admin"
                           Padding="15,12"
                           Margin="0,0,0,20"
                           KeyDown="PasswordBox_KeyDown"/>
                
                <!-- Error Message -->
                <TextBlock x:Name="ErrorTextBlock" 
                          Text="Invalid username or password" 
                          Foreground="{StaticResource ErrorRedBrush}"
                          FontSize="12"
                          Margin="0,0,0,15"
                          Visibility="Collapsed"/>
                
                <!-- Login Button -->
                <Button x:Name="LoginButton" 
                       Style="{StaticResource PrimaryButtonStyle}"
                       Content="LOGIN"
                       Padding="20,15"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Click="LoginButton_Click"
                       Margin="0,0,0,20"/>
                
                <!-- Register Link -->
                <Button x:Name="RegisterButton" 
                       Style="{StaticResource NavButtonStyle}"
                       Content="Open website to register"
                       Foreground="{StaticResource AccentBlueBrush}"
                       HorizontalAlignment="Center"
                       Click="RegisterButton_Click"/>
                
                <!-- Development Info -->
                <TextBlock Text="Dev mode: admin/admin" 
                          FontSize="11" 
                          Foreground="{StaticResource TextSecondaryBrush}"
                          HorizontalAlignment="Center"
                          Margin="0,30,0,0"
                          Opacity="0.7"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>