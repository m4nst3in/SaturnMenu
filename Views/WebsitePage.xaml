<Page x:Class="SaturnLoader.Views.WebsitePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="560" d:DesignWidth="600"
      Background="{StaticResource PrimaryDarkBrush}">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="400">
            <TextBlock Text="SATURN WEBSITE" 
                      FontSize="28" 
                      FontWeight="Bold" 
                      Foreground="{StaticResource TextPrimaryBrush}"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,20"/>
            
            <TextBlock Text="Visit our official website for registration, purchases, and support." 
                      FontSize="16" 
                      Foreground="{StaticResource TextSecondaryBrush}"
                      TextWrapping="Wrap"
                      TextAlignment="Center"
                      Margin="0,0,0,30"/>
            
            <Button Style="{StaticResource PrimaryButtonStyle}"
                   Content="OPEN WEBSITE"
                   Padding="30,15"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Click="OpenWebsite_Click"
                   HorizontalAlignment="Center"/>
            
            <TextBlock Text="saturn-loader.com" 
                      FontSize="14" 
                      Foreground="{StaticResource AccentBlueBrush}"
                      HorizontalAlignment="Center"
                      Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Page>