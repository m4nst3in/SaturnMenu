<Page x:Class="SaturnLoader.Views.InjectorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="560" d:DesignWidth="600"
      Background="{StaticResource PrimaryDarkBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30,30,30,30">
            <!-- Header -->
            <StackPanel Margin="0,0,0,30">
                <TextBlock Text="SUBSCRIPTION" 
                          FontSize="28" 
                          FontWeight="Bold" 
                          Foreground="{StaticResource TextPrimaryBrush}"
                          Margin="0,0,0,5"/>
                <TextBlock Text="Available subscriptions" 
                          FontSize="16" 
                          Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
            
            <!-- Subscription Cards -->
            <ItemsControl ItemsSource="{Binding Subscriptions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource NavButtonStyle}"
                               Padding="0"
                               Margin="0,0,0,15"
                               Click="SubscriptionCard_Click">
                            <Border Style="{StaticResource CardStyle}"
                                   Width="500"
                                   Padding="20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Card Content -->
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Name}" 
                                                  FontSize="18" 
                                                  FontWeight="SemiBold"
                                                  Foreground="{StaticResource TextPrimaryBrush}"
                                                  Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding TimeRemaining}" 
                                                  FontSize="14" 
                                                  Foreground="{StaticResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{Binding Game}" 
                                                  FontSize="12" 
                                                  Foreground="{StaticResource TextSecondaryBrush}"
                                                  Margin="0,5,0,0"
                                                  Opacity="0.7"/>
                                    </StackPanel>
                                    
                                    <!-- Game Icon -->
                                    <Border Grid.Column="1" 
                                           Width="60" Height="60" 
                                           CornerRadius="8"
                                           Background="{Binding IconColor}">
                                        <TextBlock Text="🎮" 
                                                  FontSize="24" 
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <!-- Quick Actions -->
            <StackPanel Margin="0,30,0,0">
                <TextBlock Text="Quick Actions" 
                          FontSize="20" 
                          FontWeight="SemiBold" 
                          Foreground="{StaticResource TextPrimaryBrush}"
                          Margin="0,0,0,15"/>
                
                <WrapPanel Orientation="Horizontal">
                    <Button x:Name="LoadKDButton" 
                           Style="{StaticResource PrimaryButtonStyle}"
                           Content="LOAD KD"
                           Padding="20,12"
                           Margin="0,0,15,10"
                           Click="LoadKD_Click"/>
                    
                    <Button x:Name="LoadUMButton" 
                           Style="{StaticResource PrimaryButtonStyle}"
                           Content="LOAD UM"
                           Padding="20,12"
                           Margin="0,0,15,10"
                           Click="LoadUM_Click"/>
                    
                    <Button x:Name="CopyInfoButton" 
                           Style="{StaticResource NavButtonStyle}"
                           Content="COPY INFO"
                           Padding="20,12"
                           Margin="0,0,0,10"
                           Click="CopySubscriptionInfo_Click"/>
                </WrapPanel>
            </StackPanel>
            
            <!-- Status Banner -->
            <Border Background="{StaticResource SecondaryDarkBrush}"
                   CornerRadius="10"
                   Padding="20"
                   Margin="0,30,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" 
                           Width="40" Height="40" 
                           CornerRadius="20"
                           Background="{StaticResource SuccessGreenBrush}"
                           Margin="0,0,15,0">
                        <TextBlock Text="✓" 
                                  FontSize="20" 
                                  FontWeight="Bold"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                    </Border>
                    
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="System Status" 
                                  FontSize="16" 
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  Margin="0,0,0,5"/>
                        <TextBlock Text="All systems operational" 
                                  FontSize="14" 
                                  Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>